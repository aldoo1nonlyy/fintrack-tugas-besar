name: catatan_invoice_bon_frontend
description: Frontend mobile app Flutter untuk Catatan Invoice dan Bon.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  intl: ^0.19.0
  pdf: ^3.12.0
  printing: ^5.14.3
  google_fonts: ^8.0.2
  shared_preferences: ^2.5.5
  firebase_core: ^3.15.2
  cloud_firestore: ^5.6.8
  firebase_auth: ^5.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

